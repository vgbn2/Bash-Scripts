# Network helpers

Put only custom helper scripts here, such as:

- download-limit profiles
- connectivity checks
- local-network diagnostics

The `system network` commands remain the main interface for now.
