# Historical Snapshots

Files in this directory are retained for historical reference only. They are
not loaded by the active `system` command and may not contain current safety
guards, help text, or module behavior.

Do not install or execute an archived script as the canonical command. Current
implementation lives under `system/` and its delegated production folders.
